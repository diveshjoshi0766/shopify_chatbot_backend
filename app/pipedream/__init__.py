"""
Pipedream Connect remote MCP client for the chat agent.

Submodules: `token_provider` (OAuth client credentials), `mcp_session` (streamable HTTP),
`tools` (LangChain StructuredTool wrappers). See https://pipedream.com/docs/connect/mcp/developers
"""
